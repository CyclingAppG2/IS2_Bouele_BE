%h1= @forum_thread.title
%p= @forum_thread.text

= button_to "Edit", @forum_thread, :method => :edit
= button_to "Delete", @forum_thread, :method => :delete, data: { confirm: "Estas seguro que deseas borrar el hilo?"}
